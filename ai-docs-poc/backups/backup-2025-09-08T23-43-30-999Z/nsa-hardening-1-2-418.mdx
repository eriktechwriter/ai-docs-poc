---
slug: trend-vision-one-nsa-hardening-1-2-418
title: 4.1.8 - Ensure the SELinux context of the container is set (Automated)
last_updated: '2025-09-08'
ai_context:
  source_file: nsa-hardening-1-2-418.md
  conversion_date: '2025-09-08T23:42:14.932Z'
conversion_status: converted
translation_ready: true
---

:::: {role="main"}
Profile applicability: Level 1

In Kubernetes, you can set an SELinux label in the `securityContext` field of your manifest. The specified labels are assigned to those processes. If you have configured security policies that affect those labels, the host OS kernel enforces these policies.

### Audit {#audit}

Run the following command and review the securityContext of each pod:

``` codeblock
kubectl get pods --all-namespaces
```

Ensure each pod has the SELinux context set.

### Remediation {#remediation}

To assign SELinux labels to a Container, include the `seLinuxOptions` field in the `securityContext` section of your Pod or Container manifest. The `seLinuxOptions` field is an [SELinuxOptions](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.32/) object. Here's an example that applies an SELinux level:

``` codeblock
...
securityContext:
  seLinuxOptions:
    level: "s0:c123,c456"
```

See the Kubernetes documentation for more information about [assigning SELinux labels to a container](https://kubernetes.io/docs/tasks/configure-pod-container/security-context/).

:::note

To assign SELinux labels, the SELinux security module must be loaded on the host operating system.
:::
::::
