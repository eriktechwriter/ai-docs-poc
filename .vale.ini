# Minimal Vale configuration to prevent VSCode extension errors
# Vale is currently disabled for this project

StylesPath = .vale/styles

MinAlertLevel = error

# Minimal configuration - no active rules
[*.{md,mdx}]
# No styles enabled to effectively disable Vale